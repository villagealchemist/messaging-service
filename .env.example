# Server Configuration
NODE_ENV=development
PORT=8080
HOST=0.0.0.0

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=messaging_service
DB_USER=messaging_user
DB_PASSWORD=messaging_password
DB_POOL_MIN=2
DB_POOL_MAX=10

# Provider Configuration (Mock URLs)
SMS_PROVIDER_URL=http://localhost:8081/sms
EMAIL_PROVIDER_URL=http://localhost:8081/email

# Retry Configuration
MAX_RETRIES=3
RETRY_DELAY_MS=1000
RETRY_MAX_DELAY_MS=10000

# Logging
LOG_LEVEL=info
